{
  "version": 1,
  "hooks": [
    {
      "event": "preToolUse",
      "command": "echo 'Guardrail check for ${slug}'",
      "description": "Validates tool calls for Ai Security Hardening against security and cost policies",
      "timeout": 10000,
      "mode": "warn",
      "rules": {
        "blocked_tools": [
          "rm -rf",
          "DROP TABLE",
          "kubectl delete namespace"
        ],
        "require_approval": [
          "az group delete",
          "az resource delete"
        ],
        "max_cost_per_call": 0.05,
        "content_safety": {
          "enabled": true,
          "severity_threshold": 2
        },
        "allowed_azure_services": [
          "Azure AI Content Safety",
          "Azure OpenAI",
          "Azure Container Apps",
          "Azure Key Vault",
          "Azure Monitor",
          "Azure Policy"
        ],
        "pii_protection": {
          "enabled": true,
          "action": "redact"
        }
      }
    },
    {
      "event": "sessionEnd",
      "command": "echo 'Session audit for ${slug}'",
      "description": "Audit trail for Ai Security Hardening session — logs tool calls, costs, and safety events",
      "timeout": 5000,
      "mode": "info"
    }
  ]
}